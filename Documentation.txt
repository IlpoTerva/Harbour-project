Documentation for project

TODO
Testing the full pipeline that the program flow works correctly.
Now vision works (when yolo detects the license plate).
Development to jetson. Now jetson has folder harbour_project/ which has the venv all the main packages are installed there
 - installed packages:
    * faster-whisper
    * ultralytics
    * fast-plate-ocr
    * piper-tts
    * llama-cpp-python
Now these files need to be added to the jetson and test the system with that.
 - Probable issues:
    * Microphone (Not sure if jetson has ability to catch voice data)
      * Solving (probably): usb Microphone  to the jetson to capture the voice
    * speaker (Not sure if jetson has ability to produce voice)
      * solving (probably): usb speaker to the jetson to send voice.
    
    * Not sure if the GUI will work with the jetson (one should test it) 
      -> if does not work use CLI
        * works similarly to GUI but only in commandline (no visual feedback)

NOTES
Voice assistant questions are correct.
Local development (using CPU on laptop) works alright now it still can be tested more but basic cases should work

Known issues 
    - LLM parsing returns alwasy '' eventhough the transcript has correct words (license plate or words in general).
      * This causes the program flow always alert worker and the program does not continue as it should
